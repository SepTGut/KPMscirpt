{
  "rules": {
    ".read": false,
    ".write": false,
    "active_tracking": {
      ".read": true,
      ".indexOn": ["status", "driverName", "lastUpdated"],
      "$kpmId": {
        ".write": true,
        ".validate": "newData.val() == null || (newData.hasChildren(['latitude', 'longitude', 'kpmId', '_writeKey']) && newData.child('_writeKey').isString() && newData.child('_writeKey').val().length > 10 && newData.child('latitude').isNumber() && newData.child('latitude').val() >= -90 && newData.child('latitude').val() <= 90 && newData.child('longitude').isNumber() && newData.child('longitude').val() >= -180 && newData.child('longitude').val() <= 180 && newData.child('kpmId').isString() && newData.child('kpmId').val().length > 0 && newData.child('kpmId').val().length <= 100 && (!newData.hasChild('speedKmh') || (newData.child('speedKmh').isNumber() && newData.child('speedKmh').val() >= 0 && newData.child('speedKmh').val() <= 300)) && (!newData.hasChild('accuracy') || newData.child('accuracy').isNumber()) && (!newData.hasChild('heading') || newData.child('heading').val() == null || newData.child('heading').isNumber()))"
      }
    },
    "short_links": {
      ".read": true,
      ".write": false,
      "$shortId": {
        ".validate": "newData.hasChildren(['nomorKPM']) && newData.child('nomorKPM').isString()"
      }
    }
  }
}
